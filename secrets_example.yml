# Copy this file to vagrant/secrets.yml (the folder on Windows where you run vagrant up - this is the shared folder) and fill in your values
# Never commit secrets.yml to git

db_user: appuser
db_password: changeme
db_name: coursedb
docker_hub_username: yourdockerhubusername
docker_hub_token: yourdockerhubtoken